# Quick start

See the **[README](README.md)** for install, extras, CLI, templates, and run receipts.

```bash
pip install -e ".[dev]"
analysis-gui-cli codegen templates/eeg_psd.pipeline
analysis-gui-cli run templates/csv_cluster.pipeline --cwd /path/with/data.csv
```
